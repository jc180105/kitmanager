[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['cd backend && npm ci --omit=dev || npm install']

[phases.build]
cmds = ['echo "No build step needed"']

[start]
cmd = 'cd backend && npm start'
